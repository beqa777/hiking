import React from 'react';
import { TripDescriptionStyle } from './trip-description.styles';

const TripDescription = ({tripInfo}) => (
    <TripDescriptionStyle>
        hello world
    </TripDescriptionStyle>
);

export default TripDescription;